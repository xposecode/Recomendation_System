% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
]{article}
\usepackage{xcolor}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{5}
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{graphicx}
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{% scales image to fit in text height/width
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa{\textheight}{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb{\linewidth}{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi% select the smaller of both
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox{\pandoc@box}%
  \fi%
}
% Set default figure placement to htbp
\def\fps@figure{htbp}
\makeatother
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
\hypersetup{
  pdftitle={Exploratory Data Analysis: Music Dataset for Recommendation Engine},
  pdfauthor={Student ID: C5056374},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{Exploratory Data Analysis: Music Dataset for Recommendation
Engine}
\author{Student ID: C5056374}
\date{15 December 2025}

\begin{document}
\maketitle

{
\setcounter{tocdepth}{2}
\tableofcontents
}
library(dplyr) library(ggplot2) library(readr) library(tidyr)
library(knitr) library(kableExtra) library(gridExtra) library(corrplot)
library(moments)

knitr::opts\_chunk\$set( echo = FALSE, warning = FALSE, message = FALSE,
fig.align = ``center'' ) music\_data \textless- read\_csv(``data.csv'')

required\_cols \textless- c( ``acousticness'', ``artists'',
``danceability'', ``energy'', ``id'', ``liveness'', ``loudness'',
``name'', ``popularity'', ``speechiness'', ``tempo'', ``valence'' )

music\_data \textless- music\_data \%\textgreater\%
select(all\_of(required\_cols))

cat(``Rows:'', nrow(music\_data), ``\n'') cat(``Columns:'',
ncol(music\_data), ``\n'') str(music\_data)

head(music\_data, 10) \%\textgreater\% kable(``latex'', booktabs = TRUE,
caption = ``Sample Data Rows'') \%\textgreater\%
kable\_styling(latex\_options = c(``striped'', ``hold\_position'',
``scale\_down'')) missing\_summary \textless- music\_data
\%\textgreater\% summarise(across(everything(),
\textasciitilde sum(is.na(.)))) \%\textgreater\% pivot\_longer( cols =
everything(), names\_to = ``Feature'', values\_to = ``Missing\_Count'' )
\%\textgreater\% mutate(Percentage = round(Missing\_Count /
nrow(music\_data) * 100, 2))

missing\_summary \%\textgreater\% arrange(desc(Missing\_Count))
\%\textgreater\% kable(``latex'', booktabs = TRUE, caption = ``Missing
Value Summary'') \%\textgreater\% kable\_styling(latex\_options =
c(``striped'', ``hold\_position'')) initial\_count \textless-
nrow(music\_data) music\_data \textless- drop\_na(music\_data)

cat(``Initial rows:'', initial\_count, ``\n'') cat(``Rows after
cleaning:'', nrow(music\_data), ``\n'') duplicate\_rows \textless-
sum(duplicated(music\_data)) cat(``Duplicate rows:'', duplicate\_rows,
``\n'')

music\_data \textless- distinct(music\_data) numerical\_features
\textless- c( ``acousticness'', ``danceability'', ``energy'',
``liveness'', ``loudness'', ``popularity'', ``speechiness'', ``tempo'',
``valence'' )

music\_data \textless- music\_data \%\textgreater\%
mutate(across(all\_of(numerical\_features), as.numeric))

data.frame( Feature = names(music\_data), Data\_Type =
sapply(music\_data, class) ) \%\textgreater\% kable(``latex'', booktabs
= TRUE, caption = ``Data Types of Features'') \%\textgreater\%
kable\_styling(latex\_options = c(``striped'', ``hold\_position''))
numerical\_stats \textless- music\_data \%\textgreater\% summarise(
across( all\_of(numerical\_features), list( Mean = mean, SD = sd, Min =
min, Q1 = \textasciitilde quantile(.x, 0.25), Median = median, Q3 =
\textasciitilde quantile(.x, 0.75), Max = max, Skewness =
\textasciitilde skewness(.x) ), na.rm = TRUE ) ) \%\textgreater\%
pivot\_longer( everything(), names\_to = c(``Feature'', ``Statistic''),
names\_sep = ``\_'' ) \%\textgreater\% pivot\_wider(names\_from =
Statistic, values\_from = value) \%\textgreater\%
mutate(across(where(is.numeric), round, 3))

numerical\_stats \%\textgreater\% kable( ``latex'', booktabs = TRUE,
caption = ``Descriptive Statistics of Numerical Features'' )
\%\textgreater\% kable\_styling(latex\_options = c(``striped'',
``hold\_position'', ``scale\_down'')) p1 \textless- ggplot(music\_data,
aes(danceability)) + geom\_histogram(bins = 30, fill = ``steelblue'',
alpha = 0.7) + geom\_density(color = ``red'') + theme\_minimal() +
labs(title = ``Danceability Distribution'')

p2 \textless- ggplot(music\_data, aes(energy)) + geom\_histogram(bins =
30, fill = ``coral'', alpha = 0.7) + geom\_density(color =
``darkgreen'') + theme\_minimal() + labs(title = ``Energy
Distribution'')

grid.arrange(p1, p2, ncol = 2) music\_data \textless- music\_data
\%\textgreater\% mutate( popularity\_quartile = cut( popularity,
quantile(popularity, probs = seq(0, 1, 0.25)), labels = c(``Low'',
``Medium'', ``High'', ``Very High''), include.lowest = TRUE ) )

ggplot( music\_data, aes(popularity\_quartile, danceability, fill =
popularity\_quartile) ) + geom\_boxplot() + theme\_minimal() +
labs(title = ``Danceability Across Popularity Levels'') +
theme(legend.position = ``none'') correlation\_matrix \textless- cor(
select(music\_data, all\_of(numerical\_features)), use =
``complete.obs'' )

corrplot( correlation\_matrix, type = ``upper'', tl.col = ``black'',
number.cex = 0.7 ) sample\_data \textless- sample\_n(music\_data,
min(1000, nrow(music\_data)))

ggplot( sample\_data, aes( danceability, energy, size = popularity,
color = valence ) ) + geom\_point(alpha = 0.7) +
scale\_color\_gradient2(low = ``blue'', mid = ``white'', high = ``red'')
+ theme\_minimal() + labs(title = ``Multivariate Relationship
Analysis'')

\end{document}
